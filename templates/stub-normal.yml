---
meta:
  environment: NAME
  dns_root: microbosh
  network_name: redis_counter1
  security_groups:
    - webapp
  job_templates:
    - name: redis_counter
      release: redis-counter


director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: redis-counter
    version: latest
